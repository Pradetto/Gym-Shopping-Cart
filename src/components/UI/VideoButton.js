// React / Redux
import React from "react";

// CSS

// const VideoButton = (props) => {
//   return (
//     <Button size="lg" onClick={props.action}>
//       {props.children}
//     </Button>
//   );
// };

// export default VideoButton;
